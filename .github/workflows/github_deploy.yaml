# This workflow will build and push an application to a Azure Kubernetes Service (AKS) cluster when you push your code
#
# This workflow assumes you have already created the target AKS cluster and have created an Azure Container Registry (ACR)
# The ACR should be attached to the AKS cluster
# For instructions see:
#   - https://docs.microsoft.com/en-us/azure/aks/kubernetes-walkthrough-portal
#   - https://docs.microsoft.com/en-us/azure/container-registry/container-registry-get-started-portal
#   - https://learn.microsoft.com/en-us/azure/aks/cluster-container-registry-integration?tabs=azure-cli#configure-acr-integration-for-existing-aks-clusters
#   - https://github.com/Azure/aks-create-action
#
# To configure this workflow:
#
# 1. Set the following secrets in your repository (instructions for getting these can be found at https://docs.microsoft.com/en-us/azure/developer/github/connect-from-azure?tabs=azure-cli%2Clinux):
#    - AZURE_CLIENT_ID
#    - AZURE_TENANT_ID
#    - AZURE_SUBSCRIPTION_ID
#
# 2. Set the following environment variables (or replace the values below):
#    - AZURE_CONTAINER_REGISTRY (name of your container registry / ACR)
#    - RESOURCE_GROUP (where your cluster is deployed)
#    - CLUSTER_NAME (name of your AKS cluster)
#    - CONTAINER_NAME (name of the container image you would like to push up to your ACR)
#    - IMAGE_PULL_SECRET_NAME (name of the ImagePullSecret that will be created to pull your ACR image)
#    - DEPLOYMENT_MANIFEST_PATH (path to the manifest yaml for your deployment)
#
# For more information on GitHub Actions for Azure, refer to https://github.com/Azure/Actions
# For more samples to get started with GitHub Action workflows to deploy to Azure, refer to https://github.com/Azure/actions-workflow-samples
# For more options with the actions used below please refer to https://github.com/Azure/login

name: Build and deploy an app to AKS

on:
  push:
    branches: ["main"]
    paths: 
      - '**/api-request-github/**'
  workflow_dispatch:

env:
  AZURE_CONTAINER_REGISTRY: "AzureContainerRegistryMLopsMPT"
  CONTAINER_NAME: "api-github-recup-data-auto-mpt"
  RESOURCE_GROUP: "Projet_M2_MLOPS_MRO_PEN_TAN"
  #CLUSTER_NAME: "canada-central"
  #DEPLOYMENT_MANIFEST_PATH: "your-deployment-manifest-path"

jobs:
  buildAndDeployGithub:
    # if: contains(github.event.head_commit.modified, 'api-request-github/')
    permissions:
      contents: read
      id-token: write
    runs-on: ubuntu-latest
    steps:
      # Checks out the repository this file is in
      - uses: actions/checkout@v4

      # Logs in with your Azure credentials
      - name: Azure login
        uses: azure/docker-login@v1
        with:
          login-server: ${{ secrets.AZURE_REGISTRY_URL }}  # Votre ACR
          username: ${{ secrets.AZURE_REGISTRY_USERNAME }}
          password: ${{ secrets.AZURE_REGISTRY_PASSWORD }}


      # Builds and pushes an image up to your Azure Container Registry
      - name: Build Docker image
        run: docker build -t ${{ secrets.AZURE_REGISTRY_URL }}/api-request-github:latest ./api-request-github
      
      - name: Versioning
        run: |
          VERSION_FILE=./api-request-github/version.txt
          if [ ! -f "$VERSION_FILE" ]; then
            echo "0" > $VERSION_FILE
          fi
          VERSION=$(cat $VERSION_FILE)
          NEW_VERSION=VERSION+1
          echo "$NEW_VERSION" > $VERSION_FILE
      
      - name: Push Docker image
        run: docker push ${{ secrets.AZURE_REGISTRY_URL }}/api-request-github:latest

  buildAndDeployMongoApi:
    permissions:
      contents: read
      id-token: write
    runs-on: ubuntu-latest
    steps:
      # Checks out the repository this file is in
      - uses: actions/checkout@v4

      # Logs in with your Azure credentials
      - name: Azure login
        uses: azure/docker-login@v1
        with:
          login-server: ${{ secrets.AZURE_REGISTRY_URL }}  # Votre ACR
          username: ${{ secrets.AZURE_REGISTRY_USERNAME }}
          password: ${{ secrets.AZURE_REGISTRY_PASSWORD }}

      # Builds and pushes an image up to your Azure Container Registry
      - name: Build Docker image
        run: docker build -t ${{ secrets.AZURE_REGISTRY_URL }}/api-base-mongo:latest ./api-base-mongo
        env:
          MONGO_URI: "${{ vars.MONGO_URI }}"
      - name: Versioning
        run: |
          VERSION_FILE=./api-base-mongo/version.txt
          if [ ! -f "$VERSION_FILE" ]; then
            echo "0" > $VERSION_FILE
          fi
          VERSION=$(cat $VERSION_FILE)
          NEW_VERSION=VERSION+1
          echo "$NEW_VERSION" > $VERSION_FILE
      
      - name: Push Docker image
        run: docker push ${{ secrets.AZURE_REGISTRY_URL }}/api-base-mongo:latest
        env:
          MONGO_URI: "${{ vars.MONGO_URI }}"